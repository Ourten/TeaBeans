language: java
notifications:
  email: false
install: true
jdk: oraclejdk8
script:
  - gradle build
  - gradle check
cache:
  directories:
    - ~/.gradle
after_success:
  - bash <(curl -s https://codecov.io/bash)
