language: java
notifications:
  email: false
install: true
jdk: oraclejdk8
script:
  - gradle build
cache:
  directories:
    - ~/.gradle
test:
    post:
        - gradle check jacocoTestReport
        - bash <(curl -s https://codecov.io/bash)
